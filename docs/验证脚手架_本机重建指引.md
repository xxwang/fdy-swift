# 验证脚手架 · 本机重建指引

> **为什么有这页**：验证脚手架（`.build/structprobe/`）**不入库** —— `.build/` 被 `.gitignore` 覆盖，
> 这是刻意选择（不把近千行（1049 行 .swift）验证代码塞进产品仓库）。代价是它随时可能因 `rm -rf .build` 蒸发。
> 本页是它的**规格说明与重建指引**，也是短探针的唯一持久副本。
>
> ⚠️ **本页不是完整备份**。`readme_probe.swift`（453 行）与 `App.swift`（387 行）只有规格，
> 没有源码。若 `.build/` 被清空，需按本页的块清单 / 断言清单重写。**别以为文档在就等于探针在。**

---

## 一、为什么非要有它

整模块 `swiftc -typecheck` **抓不出**下面两类问题 —— 库内一切正常，只有**模块外**视角才能看见：

| 类别 | 症状 | 只有哪一关能抓 |
|---|---|---|
| 漏登记 conformance | `.fdy` 对整类值类型不可达（或静默桥接成 `FdyWrapper<NSString>`） | 跨模块 typecheck |
| 改名后外界写不出来 | 库内改名成功、编译全过，宿主侧旧写法失效 | 跨模块 typecheck |
| 下标 / API 二义性被真正消除 | 纸面推演说「已消除」，实际两条重载仍在竞争 | **反证探针**（期望报错） |
| 行为等价性 | 「改动等价」只是推导，没有运行期证据 | 双运行时行为探针 |

## 二、目录布局

```
.build/structprobe/
├── build.sh                 # 编 dylib + 探针 app + 打包签名（29 行，全文见 §四）
├── Info.plist               # 探针 app 的 plist（含 MinimumOSVersion 18.0）
├── files.txt                # build.sh 生成的 Sources 清单（**别放 /tmp**，会被系统清空）
├── App.swift                # 行为探针主程序（387 行，S1–S26）
├── Probe.app/               # 产物：可安装的模拟器 app（含 Frameworks/libFdy.dylib）
├── lib/                     # 产物：Fdy.swiftmodule + libFdy.dylib —— 跨模块 typecheck 就靠它
│
├── readme_probe.swift       # 跨模块：重放 README 全部 swift 代码块（453 行，block 00/02–23）
├── conformance_probe.swift  # 跨模块：29 条 conformance 全覆盖 + 继承路径（117 行）
├── subscript_probe.swift    # 跨模块：下标正向（27 行，全文见 §五）
├── subscript_negative.swift # 跨模块：下标**反证**，必须报错（8 行，全文见 §五）
├── bind_probe.swift         # 跨模块：bind(from:) 泛型正向（42 行，全文见 §五）
└── bind_negative.swift      # 跨模块：bind(from:)**反证**，必须报错（15 行，全文见 §五）
```

## 三、六关期望结果

| # | 关卡 | 命令 | 期望 |
|---|---|---|---|
| ① | 整模块 typecheck（252 文件） | 见 `工业级_代码治理方案.md` 附录 | **0 error / 4 条主 warning** |
| ② | 格式 | `swiftformat --lint Sources/` | **0/252** |
| ③ | 跨模块 typecheck ×4 正向 | `-I .build/structprobe/lib` 逐个编 | `readme_probe` / `conformance_probe` / `subscript_probe` / `bind_probe` 均 **0 诊断** |
| ③' | 跨模块 typecheck ×2 **反证** | 同上 | `subscript_negative` / `bind_negative` **必须报错**（见 §五 的期望错误文本） |
| ④ | 全量编译 | `zsh .build/structprobe/build.sh` | **0 error** |
| ⑤ | 双运行时行为探针 | iOS 18.0 与 26.5 各跑一遍 `diff` | **38 行逐行一致**（`S1`–`S26`） |

**① 的「4 条主 warning」别用 `grep -c 'warning:'` 数** —— 它把诊断的缩进附注行一并计入，会得到 8。
4 条构成：1 条环境级 `could not load macro '_SwiftifyImport'` + 3 条 `UIButton` 传统 inset 属性废弃。

## 四、`build.sh` 与 `Info.plist` 全文

```bash
#!/bin/zsh
# 构建 structprobe：把真实 Sources/Fdy 编成模拟器 dylib，再用探针 app 链接
# 用法：zsh .build/structprobe/build.sh
set -e
cd /Users/mac/workspace/demo/work-points/fdy-swift
SDK=$(xcrun --sdk iphonesimulator --show-sdk-path)
# ⚠️ 清单落 .build/，**不要写 /tmp** —— /tmp 会被系统整体清空
find Sources -name "*.swift" > .build/structprobe/files.txt

echo "--- 1/3 编译 Fdy dylib ---"
# -install_name 必须显式写成 @rpath/...，否则装进模拟器后会按编译时的相对路径找 dylib 而加载失败
xcrun swiftc -emit-library -emit-module -module-name Fdy \
    -target arm64-apple-ios18.0-simulator -sdk "$SDK" -swift-version 5 \
    @.build/structprobe/files.txt \
    -Xlinker -install_name -Xlinker @rpath/libFdy.dylib \
    -emit-module-path .build/structprobe/lib/Fdy.swiftmodule \
    -o .build/structprobe/lib/libFdy.dylib 2>&1 | grep -v "incompatible-sysroot" || true

echo "--- 2/3 编译探针 app ---"
xcrun swiftc -parse-as-library -target arm64-apple-ios18.0-simulator -sdk "$SDK" -swift-version 5 \
    -I .build/structprobe/lib -L .build/structprobe/lib -lFdy \
    -Xlinker -rpath -Xlinker @executable_path/Frameworks \
    .build/structprobe/App.swift -o .build/structprobe/Probe.app/Probe 2>&1 | grep -v "incompatible-sysroot" || true

echo "--- 3/3 打包签名 ---"
cp .build/structprobe/lib/libFdy.dylib .build/structprobe/Probe.app/Frameworks/
cp .build/structprobe/Info.plist .build/structprobe/Probe.app/
codesign --force --sign - .build/structprobe/Probe.app/Frameworks/libFdy.dylib 2>&1 | tail -1
codesign --force --sign - .build/structprobe/Probe.app 2>&1 | tail -1
echo "构建完成"
```

`Info.plist` 要点（缺一不可）：

| 键 | 值 | 为什么 |
|---|---|---|
| `CFBundleIdentifier` | `com.wbtest.structprobe` | `simctl launch` 要用它 |
| `CFBundleExecutable` | `Probe` | 须与 `-o .../Probe` 输出名一致 |
| `MinimumOSVersion` | `18.0` | 与包门槛对齐 |
| `UILaunchScreen` | 空 `<dict/>` | **可省**。实测（2026-09-18）删掉它后 app 仍正常启动、38 行断言全出 —— 「缺它拒绝启动」的说法在本探针上不成立。保留只为与真机 / 上架要求对齐 |

## 五、短探针全文（这六份是持久副本）

### `subscript_probe.swift` —— 下标正向

```swift
import Fdy

// 正向：两条下标各司其职，不再靠上下文返回类型消解
func probeString(_ s: String) {
    // String 专有：安全子串（越界返回 nil）
    let _: String? = s[fdy_range: 0 ..< 2]
    // RRC 通用：切片
    let _: Substring = s[fdy_slice: 0 ..< 2]
}

func probeArray(_ a: [Int]) {
    let _: ArraySlice<Int> = a[fdy_slice: 1 ..< 3]
}

func probeMutate() {
    var array = [1, 2, 3, 4]
    array[fdy_slice: 1 ..< 3] = [9, 9]

    var str = "Hello"
    str[fdy_range: 0 ..< 5] = "Hi"
    str[fdy_slice: 0 ..< 2] = "Yo"
}

// String 的 NSRange 版仍用 fdy_range（NSRange 不是 RangeExpression，与切片版无冲突）
func probeNSRange(_ s: String) {
    let _: String? = s[fdy_range: NSRange(location: 1, length: 3)]
}
```

### `subscript_negative.swift` —— 下标反证

```swift
import Fdy

// 反证：二义性若已消除，本文件**必须编译失败**。
// 期望报错：cannot convert value of type 'String?' to specified type 'Substring'
//          （若仍能编译，说明 RRC 的泛型下标仍在用 fdy_range 标签，二义性未消除）
func negative(_ s: String) {
    let _: Substring = s[fdy_range: 0 ..< 2]
}
```

### `bind_probe.swift` —— `bind(from:)` 正向

```swift
import Combine
import Fdy
import UIKit

private final class ProbeVM {
    @Published var name: String?
    @Published var flag: Bool = false
}

@MainActor
func bindProbe() {
    let textField = UITextField()
    let label = UILabel()
    let switchView = UISwitch()
    let scrollView = UIScrollView()
    let vm = ProbeVM()

    // 1) 向后兼容：AnyPublisher 照旧可传
    let erased: AnyPublisher<String?, Never> = Just("a").eraseToAnyPublisher()
    textField.fdy_textPublisher.bind(from: erased)

    // 2) Subject —— 此前必须 .eraseToAnyPublisher()
    let subject = CurrentValueSubject<String?, Never>("b")
    label.fdy_textPublisher.bind(from: subject)

    // 3) Published.Publisher —— 此前必须 .eraseToAnyPublisher()
    textField.fdy_textPublisher.bind(from: vm.$name)

    // 4) 中间流（map + filter）—— 此前无 erase 根本编不过
    textField.fdy_textPublisher.bind(from: subject.map { $0?.uppercased() }.filter { _ in true })

    // 5) 库自有 FdyControlEvent，Output 匹配时**直接传**，连 map 都不用
    switchView.fdy_isOnPublisher.bind(from: scrollView.fdy_didEndDraggingWithDecelerationPublisher)

    // 6) FdyControlEvent 经 map 换 Output 后照样可传
    switchView.fdy_isOnPublisher.bind(from: textField.fdy_tapPublisher.map { _ in true })
}
```

### `bind_negative.swift` —— `bind(from:)` 反证

```swift
import Combine
import Fdy
import UIKit

// 反证：Failure 非 Never 的上游**必须**编不过。
// 若本文件编译通过，说明 bind(from:) 被放宽到了不该放宽的程度。
private enum ProbeError: Error { case boom }

@MainActor
func bindNegative() {
    let textField = UITextField()
    let failing = Fail<String?, ProbeError>(error: .boom)
    textField.fdy_textPublisher.bind(from: failing)
}
```

期望错误：`error: instance method 'bind(from:)' requires the types 'ProbeError' and 'Never' be equivalent`

## 六、长探针的规格（无源码，需重写）

### `readme_probe.swift` —— README 全代码块重放

按 README **出现顺序**逐块重放 `swift` 代码块，要求跨模块 0 诊断。覆盖 `block 00` 与 `block 02`–`block 23`
（共 25 块；`block 01` 是 `Package.swift` 片段，非 Swift 源码，跳过）。

**改写原则（破坏它验证就失效）**：只补**最小上下文** —— 未定义符号 → 局部声明、伪代码 `...` → 真实表达式、
主线程入口 → `@MainActor`。**绝不改 API 调用形态**。
这份探针的价值就在于「README 里的写法**原样**能编过」，一旦顺手「修一下」就变成自欺。

### `conformance_probe.swift` —— 29 条 conformance 全覆盖

核心手法：**显式类型标注收口**，`let _: FdyWrapper<X> = x.fdy`。
只写 `let w = x.fdy` 是**没用的** —— 推断会跟着实际类型走，静默桥接了也看不出来（`String` → `NSString` 那种）。

必须覆盖三类边界：

1. 全部 29 条登记类型（清单见 `工业级_代码治理方案.md` §8.3 ① 或 README「协议」一节）
2. **CF 继承路径**：`CGPath` 显式登记 + `CGMutablePath` **不登记**，断言子类也能 `.fdy` 且能用父类链式方法
3. 嵌套泛型容器：如 `FdyWrapper<[String: [Int]]>`
4. `with` / `do` / `build` / `then` 四件套至少各一次

### `App.swift` —— 行为探针（S1–S26）

`@main` + `UIApplicationDelegate`，`didFinishLaunching` 里 `asyncAfter(0.3)` 调 `run()`，
开头 `setbuf(stdout, nil)`（否则 `--console-pty` 抓不到输出）。每条断言 `print("S<n> ...")`，
末行 `print("DONE")`。

| 断言 | 验证什么 |
|---|---|
| S1 | `FdyButton(frame:)` 库外可构造（改造前是 internal） |
| S2 | 子类 hook：`init(frame:)` 路径下 `setupUI`/`bindEvents` 各调 1 次 |
| S3 | 类工厂返回**子类**（改造前返回 `FdyButton`，子类成员拿不到） |
| S4 | 基类工厂未回归 |
| S5 | 热区 + 防重复点击的链式入口未回归 |
| S6 | 热区命中判定生效（容器内 20×20 按钮向外各扩 20pt） |
| S7 | `backgroundColor(_:for:)` 可用，与 `backgroundImage` 不冲突 |
| S8 | 权限类型新名 + 便利别名 |
| S9 | 触觉反馈的新嵌套名 |
| S10 | 元组容器新名 |
| S11 | 新增 `fdyG` 入口（类型存在性 + 单例同一性 `===`） |
| S12 | Combine 新类型名 |
| S13 | `FdyTextView` 与 `FdyButton` 组件约定一致（类工厂返回子类 + coder 初始化器可用） |
| S14 | 补 conformance 后三个原本不可达的类型链式可用 |
| S15 | `"abc".fdy` 是 `FdyWrapper<String>`（修病：NSString 桥接导致类型错） |
| S16 | 越界截取不再因 `Range` 构造 trap（修复前 `fdy_substring(from: 10)` 于 `"abc"` 直接崩） |
| S17 | `contentOffset` 直传（不裁剪）vs `contentOffsetClamped`（上界含 bounds 修正） |
| S18 | 滚动定位统一考虑 `contentInset`（原 `scrollToEndTop` 已并入 `scrollToTop`） |
| S19 | 段落样式增量修改（不互相覆盖，且不改写 `alignment`） |
| S20 | 文本首尾滚动按 UTF-16 计算（含 emoji 不越界） |
| S21a/b/c | 配置化按钮上传统 setter 的真实表现：`title` 不生效、`font` 生效、`titleColor` 不生效 |
| S22 | 子类删除重复定义后，滚动方法仍经父类扩展可达 |
| S23a | `bind(from:)` 直传 `Subject` → 值写回控件 |
| S23b | `bind(from:)` 直传 `@Published` → 值写回控件 |
| S23c | `bind(from:)` 直传 `FdyControlEvent`（Bool→Bool）→ 事件触发后值写回 |
| S24a | 水印移除后**宿主自建的同名** `"dy.watermark"` 图层仍在（旧实现按 `name` 删会误伤） |
| S24b | `fdy_stopEmitter()` 后**宿主自建的无名** `CAEmitterLayer` 仍在（旧实现那个 `|| name == nil` 会一并删掉） |
| S25a | 7 个手势访问器各自建出识别器并挂载（`gestureCount=7`，缓存生效、全部 `addGestureRecognizer` 成功） |
| S25b | 手势 publisher 的 action 派发 —— **本探针手段下恒 `false`，非库缺陷**，见 S25c |
| S25c | **对照实验**：手工 `addTarget` 的 target 同样不触发 → 证明私有 `_setState:` 只改 state、不派发 action |
| S26a | 从识别器私有 ivar `_targets`（`NSMutableArray`，KVC 取值安全）取出库注册的 target，正向调 `invoke:` → **7 个识别器全部 `1/1` 派发成功** |
| S26b | **7 个手势 publisher 全部 `true`** → `识别器 target-action → ClosureTarget.invoke(_:) → flush → subscriber.receive → sink` 整条链路被证明成立（`S25b` 的全 `false` 由此确认只是 `_setState:` 手段的局限） |
| S26c | 对照 target 同法调用 → `targetFired=true`，排除「该手法只对库的 target 有效」的单边偏差 |

**S21 必须让 runloop 转圈再读**（`RunLoop.main.run(until: Date().addingTimeInterval(0.2))`）：
UIKit 的 configuration → titleLabel 桥接是**异步**的，立刻读会得到假 `nil`。

**S23c 的触发方式**：`scrollView.delegate?.scrollViewDidEndDragging?(scrollView, willDecelerate: true)`
—— 直接调 delegate 方法即可，无需真的触摸。

## 七、重建步骤

```bash
cd /Users/mac/workspace/demo/work-points/fdy-swift
mkdir -p .build/structprobe/{lib,Probe.app/Frameworks}

# 1. 写 Info.plist（见 §四 要点）与 build.sh（见 §四 全文），并 chmod +x
# 2. 写 6 个探针文件（短探针照 §五 抄，长探针照 §六 规格重写）
# 3. 写 App.swift（照 §六 S1–S26 清单）

zsh .build/structprobe/build.sh          # ④ 全量编译

# ③ 跨模块 typecheck（正向 x4）
SDK=$(xcrun --sdk iphonesimulator --show-sdk-path)
for p in readme_probe conformance_probe subscript_probe bind_probe; do
  echo "== $p"
  xcrun swiftc -typecheck -target arm64-apple-ios18.0-simulator -sdk "$SDK" -swift-version 5 \
    -I .build/structprobe/lib .build/structprobe/$p.swift
done
# ③' 反证 x2（**期望报错**）
for p in subscript_negative bind_negative; do
  xcrun swiftc -typecheck -target arm64-apple-ios18.0-simulator -sdk "$SDK" -swift-version 5 \
    -I .build/structprobe/lib .build/structprobe/$p.swift
done

# ⑤ 双运行时行为探针
D=.build/structprobe
run_probe() {   # $1 = UDID, $2 = 原始输出文件
  xcrun simctl boot $1 2>/dev/null
  xcrun simctl bootstatus $1 -b >/dev/null 2>&1          # ← 少了这步会得到**假 IDENTICAL**
  xcrun simctl terminate $1 com.wbtest.structprobe 2>/dev/null
  xcrun simctl install $1 $D/Probe.app || return 1
  ( xcrun simctl launch --console-pty --terminate-running-process \
      $1 com.wbtest.structprobe > $2 2>&1 & echo $! > $D/pp )
  sleep 25; kill $(cat $D/pp) 2>/dev/null
  xcrun simctl terminate $1 com.wbtest.structprobe 2>/dev/null
  grep -E '^S|^DONE' $2
}
run_probe 897DFB7B-177D-4A9A-9C1B-B924C3EA9DF5 $D/ios18_raw.txt  > $D/probe_ios18.txt   # iOS 18.0
run_probe 21956BBD-1171-453D-B94E-0D06CFA3F350 $D/ios265_raw.txt > $D/probe_ios265.txt  # iOS 26.5
wc -l < $D/probe_ios18.txt        # ← 必须非 0，否则下面的 IDENTICAL 是假的
diff $D/probe_ios18.txt $D/probe_ios265.txt && echo IDENTICAL
```

UDID 会变（换 Xcode / 删设备即失效），用 `xcrun simctl list devices available` 现取。

## 八、工具级坑（都踩过，别再试）

- **模拟器处于 Shutdown 时 `simctl install` 直接失败** → 输出文件为空 → `diff` 得到**假 IDENTICAL**。
  必须 `boot` + `bootstatus -b`，且判定一致性前**校验行数非零**。
- **探针 app 是常驻 `UIApplication`，`--console-pty` 不会自己退出** —— 必须后台起 + 定时 `kill`，
  否则一直挂到工具超时（会被误判成构建失败）。
- **别把任何中间产物写 `/tmp`** —— 会被系统整体清空（本脚手架的历史探针已蒸发过一次）。
- **`grep -c 'warning:'` 会把诊断的缩进附注行一起数进去** —— 4 条主诊断数成 8。
  判基线漂移看 `grep -E 'warning:' | sed -E 's/^[^ ]+:[0-9]+:[0-9]+: //' | sort -u`。
- **macOS BSD grep 的 BRE 不支持 `\|`，也不支持 `\s`** —— `grep -n 'a\|b'` **静默当字面量**返回空。
  **一律 `grep -E`**，空白类写 `[[:space:]]`。
- **跨模块探针报满屏 `cannot find type` 时先看有没有 `import Fdy`** —— 那是假阴性，不是 API 不可达。
- **`swiftc` 整模块 typecheck 抓不出漏登记 conformance** —— 必须走 §三 的 ③。
