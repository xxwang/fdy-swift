---
description: 跨项目通用工程规范 —— 完成判据、工具陷阱红线、注释/命名/文档通则
alwaysApply: true
---

# 通用工程规范

> **判据清单，不写推导**。适用于所有项目；**项目专属判据在各仓库根的 `AGENTS.md`**。
> 协作约定（改码前先问、提交由用户执行、称呼）在人设档与用户记忆里，此处不重复。

## 1 · 完成判据：必须实跑

- 声明「完成 / 通过」前**必须真跑**验证命令，并附上命令与输出；**没跑不得声明**。
- 判据一律写**期望值**（「0 error」「0 命中」「N/N 一致」），不写「看着没问题」。任一项不达标 = 未完成。
- 改动前先说清楚改什么、怎么改，等确认再动手；**只读排查 / 诊断 / 分析不受限**。
- 写「已落地」前必须 `git status` 逐项实核，**不凭记忆**。

## 2 · 假绿红线（逐条都真踩过）

### shell 与检索

- ⛔ **zsh 不对未加引号的变量做词分割**：`POS="a b c"; for p in $POS` 里 `$p` 是**整串** → 命令拿到不存在的文件名 → 报错不匹配诊断正则 → 数出 **0** → 看着全绿、实际一个都没跑。修法 **`POS=(a b c)`**；**自证判据：循环必须打印出预期行数**。
- ⛔ **zsh 里 `status` 是只读特殊参数**，脚本里改写会失败 ⇒ 换名（如 `st`）。
- ⛔ **`grep -c` 无匹配时 exit = 1**，会静默截断 `&&` 链（期望 0 命中的检查反而让后续验证全没执行）⇒ 后续命令一律用 `;` 接。
- ⛔ **量化一律 `grep -E`**：BSD grep **不支持 `\|` 交替、不支持 `\s`**，两者都**静默返回空 + exit 1**（看起来就像「文件里没这内容」）。要空格用 `[[:space:]]`。
- ⛔ **基于 ripgrep 的检索工具默认跳过隐藏目录**（`.github/` 这类）→ **静默报「无匹配」** ⇒ 用 shell `grep -rn`。**「无匹配」在隐藏目录场景下不是证据。**
- ⛔ **`--apply` 类改写脚本一律不接管道**：`… --apply | head -3` 的 SIGPIPE 会打断收尾 ⇒ 部分文件未处理。**验收 = 连跑两次都 0 命中。**

### 文件编辑

- ⛔ **`Edit` 改完必须回读复核**（会偶发报 success 但文件未变）。
- ⛔ **同一文件的多个 `Edit` 绝不并行发起**：只有最后一个落盘，前面的改动静默丢失 —— 而每个都返回了 success ⇒ 串行改 + 每次改完 `grep -c` / `Read` 验落盘。
- ⛔ **插入式补全（在文件末尾顶层 `}` 前插内容）后必须 `git diff` 逐文件核删除行数**：脚本会静默截断顶层 `}` 之后的内容 ⇒ **「纯新增」批次也要查有没有删到东西**。插入脚本用「内容片段」定位、**绝不用行号**；必须**幂等**；跑前先 dry-run。

### 判据与统计

- ⛔ **`git diff --numstat` 的删除数 ≠ 本次改动**：工作区常带前几批未提交改动 ⇒ 核对本次改动要看**行级 diff 内容**。
- ⛔ **别拿上一轮审计清单当施工图**（旧清单实测常有：符号不存在 / 位置串了 / 行号偏了）⇒ 每项重新定位。
- ⛔ **「判某符号还在不在」必须用代码里的当前名 + 全库搜**：改名后旧名零命中 **≠** 不存在。搜索范围须含源码、README、CHANGELOG —— 只搜源码目录会漏掉文档里的改名记录。

### 进程与性能

- ⛔ **`exit 137` 先怀疑超时 / 性能，别默认甩给沙箱**：看脚本**进度行有没有出现** —— **零输出 + 立即 137 = 早死**（真被 kill）；**跑一半才 137 = 超时**（性能问题）。方向判错会白跑一轮。
- ⛔ **性能归因必须先分离 IO 与 CPU**：瓶颈常在**重复读盘**不在 CPU（实测单次读盘约 **72 ms**、同文件单次解析仅 **0.24 ms**，差 300 倍）。修法是**把读盘提到循环外**（某哨兵 2601 次读盘 → 291 次，176 s → 17 s，输出逐字节一致）。抱怨「沙箱慢」前，先量一次单次 IO 的绝对耗时。
- ⛔ **编译 / 构建失败时旧产物仍在** ⇒ 探针照样跑出**旧结果**（假绿）。修法：**显式删产物 + 判退出码**；脚本里 `… | grep … || true` 会吞掉编译失败。
- ⛔ **跑 diff 类比对前必须显式清掉两侧旧输出**：`rm -f` 只拦「本轮没产出 → 空输出」，**拦不住上一轮的陈旧非空结果**（曾拿旧文件报出一致）。
- ⛔ **依赖后台设备的验证要等状态就绪**（如模拟器须 `boot` + `bootstatus -b`）：设备在 Shutdown 时安装会**直接失败 → 输出空 → diff 假报一致**。
- ⛔ **读运行时状态必须先让事件循环转圈**，立刻读会得到假 `nil`。

### 产物卫生

- ⛔ **别在脚本里 `py_compile` 复核后直接 `git add`**：会产 `__pycache__/*.pyc` 并带入库。入库前查 `git ls-files | grep -E '\.pyc$|__pycache__'`；已入库的要 `git rm -r --cached` 清（`.gitignore` 只管未来）。
- ⛔ **沙箱内 `git push` 会静默失败且 exit 0** ⇒ 代跑推送必须免沙箱，并用 `git ls-remote` 实核 hash。
- ⛔ **各种工具产物**：确认落盘用 `Read` 或计数命令，别信「应该有」。

## 3 · 注释通则

- **判据：签名说得出的不写，签名说不出的才写**；「一句能说清就别写一段」。
- 该删的只有**大块说明**：代码围栏、`- Example:` 段、文件头论文、实现过程走查、历史叙述、基础常识解释。
- **标准格式的完整性优先于「不冗余」**：签名里说不出的约束、标准 doc tag（`- Parameter` / `- Returns` / `- Throws`）照写。
- **公开 API 上看似复述的短文档要保留** —— 那是 `help()` / 文档站的输出，**给使用者看的文字与给自己看的注释不是一回事**。
- ⛔ **公开库不做「零引用 ⇒ 可删」的自动清点**：`public` 成员零内部引用是**正常的**（它就是产品面）；只有类型别名与 `private` / `fileprivate` / `internal` 零引用才是真死。
- ⛔ **属性包装器会「切断」文档注释**：`@available` / `@discardableResult` 插在 `///` 与声明之间时，文档注释应写在包装器**之前**；按「紧邻上一行」判摘要的解析器会误判成「无摘要」。
- 改注释的**不变量**：**文件清单不变 · 代码 token 逐行相同 · 格式化检查与编译判据不退化**。核对器分两档：严格档（纯代码行逐行相同）+ token 档（剥空白后逐行相同）—— 只跑严格档会把格式化器的空白修正误报成「有语义改动」。

## 4 · 命名通则

- **转换类方法命名**：**新名 = 前缀 + 目标类型原名**，类型前缀一律保留（`toUIImage()` / `toCGPoint()` / `toNSRange()`）。
  - **不适用**（保持原样）：`static` 工厂 / `static` 取值（解析类）、区间构造、语义化名、**格式化 / 编码 / 截取 / 派生**（`hexString` / `jpegBase64String` / `subData` …）—— 限定词才是信息量所在，加 `to` 反而失真。
- ⛔ **禁止全库替换**：常有同名符号只有**部分定义**要改 ⇒ `sed -i 's/a/b/g'` 必然误伤。**必须逐条对照映射表**。
- **改名前先查该类型 / 文件的历史增删记录**，避免与既往的「删除决议」打架。
- 为「统一」而削掉信息量是**负收益**：新名以语义完整为准。

## 5 · 文档分层（规范往哪写）

| 层 | 位置 | 入库 | 放什么 |
|---|---|---|---|
| **通用规范** | 本机用户级规则目录（跨项目自动加载） | 否 | 跨项目判据 —— 本文件的落点 |
| 通用规范副本 | 各仓库的 `CONVENTIONS.md` | 是 | 同源副本，供入库分发 |
| **项目规范** | 仓库根 `AGENTS.md` | 是 | 项目专属判据 / 命令 / 契约 |
| 明细 | 仓库内文档目录 | 看项目 | 推导、历史批次、实测数据 |

规则：

1. **判据上提规范，推导留在明细**。新产出先问「**这是判据吗**」→ 是 → 进规范；是推导 → 明细档；是本机事实 → 本机记忆档。
2. ⛔ **同一份判据不许两处并存**（必然漂移）—— 定一处为权威源，别处只放指针。
3. ⛔ **规范正文不写「见某文件」代替判据**：首轮注入**不展开导入**，会直接失效 ⇒ 判据必须**内联**。
4. ⛔ 规范文件有**体积上限**，且**超限是整份丢弃、不是截断** ⇒ **最致命的判据必须保持在文件前部**。
5. **一次性方案 / 审计记录用完即弃**：结论一旦并入规范，原档即可移除；有长期价值的内容要**主动上提**。
6. 对外文件（README / CHANGELOG）**不得出现内部文档路径** —— 用「内部治理方案」这类无路径表述。
